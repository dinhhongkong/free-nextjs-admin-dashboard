/**
 * Chuyển đổi số double thành int
 * @param {number} num - Số double cần chuyển đổi
 * @returns {number} - Số nguyên sau khi chuyển đổi
 */
export function doubleToInt(num) {
  return Math.floor(num);
}